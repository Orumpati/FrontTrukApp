import { ViewshipperconPipe } from './viewshippercon.pipe';

describe('ViewshipperconPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewshipperconPipe();
    expect(pipe).toBeTruthy();
  });
});
